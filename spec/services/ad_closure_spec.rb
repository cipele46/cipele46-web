require 'spec_helper'

describe AdClosure do
  subject { AdClosure.new }
  describe "#call" do
    it "accepts ad id and user and closes the given ad" do
      user = double(:user)
      ads = double(:ads)
    end
  end
end
